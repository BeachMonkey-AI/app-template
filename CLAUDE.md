<!--
  Thin by design — repo-specific facts only. Architecture, tokens, build mechanics,
  and CI live in this template's README.md (which this app inherited a copy of;
  it isn't linked, so edit this file freely without touching the source template).
  Delete this comment once filled in.
-->

# CLAUDE.md — <app name>

- **What this app does:** <one paragraph>
- **Live:** <url>
- **Model:** vanilla (single-file-ish, CDN deps, no bundler) | vite (React/Tailwind → dist/)
- **Token deviations from app-template:** <e.g. "--accent overridden to #38bdf8"; "none">
- **Anything else Bob needs to know that isn't obvious from the code:**
